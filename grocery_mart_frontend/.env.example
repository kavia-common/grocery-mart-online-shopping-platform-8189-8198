# Frontend environment example (to be mapped to window.__env at deploy time)
# The deployment platform should inject these into a script that sets window.__env.
API_BASE_URL=https://your-backend.example.com/api
